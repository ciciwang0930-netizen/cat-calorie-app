# 喵热量 · 猫咪每日热量记录 App — PRD

> 版本：v0.2（已按反馈修订） | 日期：2026-08-06 | 状态：待最终确认
> 形态：PWA（单页应用，添加到主屏幕使用，无需上架）
> v0.2 变更：取消多猫管理；记录方式改为「选餐包 + 吃的比例」；进度条改横长条；色调定为粉色低饱和少女系；猫粮库支持批量录入

---

## 1. 产品概述

仅供个人使用的单猫热量记录工具。猫粮库提前录入每包/每罐热量，喂食时只需**选食物 + 选吃了百分之几**，自动换算实际摄入热量。首页横长条进度条直观展示「今天还能吃多少」。

**核心体验**：打开 App → 点食物 → 点比例（100% / 60%…）→ 完成，5 秒记一笔。

## 2. 用户与场景

| 项目 | 说明 |
|---|---|
| 用户 | 本人 + 一只猫，单用户无账号 |
| 典型场景 | ① 开一袋酱包/餐包，猫吃了几成，随手记一笔；② 喂食前看一眼横条进度决定给多少；③ 换粮/买新餐包时批量录入猫粮库 |
| 设备 | iPhone，PWA 添加到主屏幕，全离线 |

## 3. 功能需求

### P0 — 核心闭环

**F1 首页 · 今日看板**
- 顶部：今日日期 + 已摄入 / 目标 kcal
- 主视觉：**横向长条进度条**（奶粉色填充、圆角胶囊形、带爪印刻度等装饰）
  - 进度条上方/中央：可爱表情（随进度变化 😸→😺→🙀）+ **「还能吃 XX kcal」超大数字**
  - 超量：进度条变深粉/红色，表情变 🙀，显示「已超出 XX kcal」
- 今日喂食时间线：时间、食物名、比例、kcal，左滑删除
- 底部大按钮「记一餐 🐾」

**F2 喂食记录（核心流程）**
1. 点「记一餐」弹出记录面板
2. 从猫粮库选食物：常用置顶 + 搜索，按类型分组（酱包/干粮/罐头/零食）
3. 选吃了多少：
   - 餐包类 → 比例快捷按钮 **100% / 75% / 50% / 25%** + 自定义滑块，实时显示换算 kcal
   - 干粮类（按克计）→ 直接输入克数
4. 保存，回首页看进度条动画增长
- 换算公式：`实际 kcal = 单包 kcal × 比例%` 或 `每100g kcal × 克数 / 100`
- 支持补录（改时间）、支持临时手动记一笔（不入库，直接填 kcal，用于偶尔给的零食）

**F3 猫粮库**
- 字段：名称、类型（酱包/干粮/罐头/零食/其他）、热量基准（**每包 kcal** 或 **每100g kcal**）、规格备注（如「85g/包」）、使用次数
- 单条新增/编辑/删除、搜索
- **批量录入**：多行文本粘贴，每行一条，格式：
  ```
  顽皮鲜封包鸡肉味, 酱包, 85
  皇家室内成猫粮, 干粮100g, 380
  小李子罐头牛肉, 罐头, 120
  ```
  解析预览 → 确认入库；格式错误的行高亮提示不阻塞其他行
- 首次打开预置 3~5 条示例数据（可删）

**F4 目标设置**
- 每日热量目标：手动输入数字即可
- 辅助参考：输入当前体重，显示 RER 建议值（70 × kg^0.75 × 系数），一键采用
- 猫咪资料：名字 + 头像表情（单猫，仅展示用）

### P1 — 增强

**F5 体重记录与曲线**：定期录入体重，SVG 折线图趋势
**F6 历史日历**：每天一格，达标✅/超标🔴/未记⚪；周均摄入
**F7 数据备份**：导出/导入 JSON

### 明确不做

- 多猫管理、账号、云同步、扫条码、推送提醒

## 4. 设计风格 — 粉色少女系

| 项 | 规格 |
|---|---|
| 色调 | 低饱和粉色系：奶油底 #FFF5F7、主色奶粉 #F8BBD0 系、点缀蜜桃 #FFD9C0 / 薄雾紫 #E6DEF0，忌高饱和玫红 |
| 形状 | 大圆角卡片（≥20px）、胶囊按钮、胶囊进度条 |
| 装饰 | 内联 SVG：爪印、小鱼干、蝴蝶结、手绘猫头，散布在留白处 |
| 字体 | 系统圆体感字重，剩余 kcal 数字特大加粗 |
| 微交互 | 保存成功时进度条液体动画涨上去 + 表情跳动 + 撒小爪印 |
| 表情状态 | 😸 进度 <50%「还很充裕」/ 😺 50~90% / 😻 90~100%「刚刚好」/ 🙀 >100%「超量啦」 |

## 5. 页面结构

```
底部 Tab（3 个）
├── 🏠 今日   — 横条进度 + 剩余kcal + 喂食时间线 + [记一餐🐾]大按钮
├── 🍖 猫粮库 — 搜索 + 分组列表 + 新增/批量录入
└── 🎀 我的   — 猫咪资料 / 每日目标设置 / 体重曲线 / 日历 / 数据导出导入
```

## 6. 数据模型（localStorage）

```json
{
  "profile": { "name": "猫咪名", "avatar": "😺", "targetKcal": 250, "weightKg": 4.2 },
  "foods": [{ "id", "name", "type": "酱包|干粮|罐头|零食|其他",
              "kcalPerPack": 85,        // 餐包类
              "kcalPer100g": null,      // 干粮类
              "spec": "85g/包", "useCount": 12 }],
  "meals": [{ "id", "foodId|null", "customName|null",
              "percent": 60,            // 餐包类记录比例
              "grams": null,            // 干粮类记录克数
              "kcal": 51, "timestamp": 0 }],
  "weights": [{ "id", "kg", "date" }]
}
```

## 7. 技术方案

- 单文件 `index.html`（内联 CSS/JS/SVG）+ manifest + Service Worker，全离线
- 无外部依赖，图表自绘 SVG
- iOS Safari → 分享 → 添加到主屏幕

## 8. 验收标准

1. 记一餐全流程 ≤ 4 次点击（选食物→选比例→保存）
2. 比例换算准确：85kcal 餐包吃 60% = 51 kcal
3. 批量录入 10 行文本一次入库，格式错误行有提示
4. 进度条随记录实时动画更新，超量变色变表情
5. 离线可用、杀进程重开数据不丢、JSON 导出可完整还原
6. 粉色低饱和少女感，你验收

## 9. 里程碑

| 阶段 | 内容 |
|---|---|
| M1 | ✅ PRD 确认 |
| M2 | 单文件 PWA 完成 P0（首页/记录/猫粮库/目标设置） |
| M3 | 真机试用迭代 + P1（体重/日历/备份） |
